/**
 * JsonHub API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v0.8.7
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DefinitionDefinitionWrite model module.
 * @module model/DefinitionDefinitionWrite
 * @version v0.8.7
 */
class DefinitionDefinitionWrite {
    /**
     * Constructs a new <code>DefinitionDefinitionWrite</code>.
     * 
     * @alias module:model/DefinitionDefinitionWrite
     * @param jsonSchema {Object} 
     */
    constructor(jsonSchema) { 
        
        DefinitionDefinitionWrite.initialize(this, jsonSchema);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, jsonSchema) { 
        obj['jsonSchema'] = jsonSchema;
    }

    /**
     * Constructs a <code>DefinitionDefinitionWrite</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DefinitionDefinitionWrite} obj Optional instance to populate.
     * @return {module:model/DefinitionDefinitionWrite} The populated <code>DefinitionDefinitionWrite</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DefinitionDefinitionWrite();

            if (data.hasOwnProperty('slug')) {
                obj['slug'] = ApiClient.convertToType(data['slug'], 'String');
            }
            if (data.hasOwnProperty('jsonSchema')) {
                obj['jsonSchema'] = ApiClient.convertToType(data['jsonSchema'], Object);
            }
            if (data.hasOwnProperty('parentEntity')) {
                obj['parentEntity'] = ApiClient.convertToType(data['parentEntity'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DefinitionDefinitionWrite</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DefinitionDefinitionWrite</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DefinitionDefinitionWrite.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['slug'] && !(typeof data['slug'] === 'string' || data['slug'] instanceof String)) {
            throw new Error("Expected the field `slug` to be a primitive type in the JSON string but got " + data['slug']);
        }
        // ensure the json data is a string
        if (data['parentEntity'] && !(typeof data['parentEntity'] === 'string' || data['parentEntity'] instanceof String)) {
            throw new Error("Expected the field `parentEntity` to be a primitive type in the JSON string but got " + data['parentEntity']);
        }

        return true;
    }


}

DefinitionDefinitionWrite.RequiredProperties = ["jsonSchema"];

/**
 * @member {String} slug
 */
DefinitionDefinitionWrite.prototype['slug'] = undefined;

/**
 * @member {Object} jsonSchema
 */
DefinitionDefinitionWrite.prototype['jsonSchema'] = undefined;

/**
 * @member {String} parentEntity
 */
DefinitionDefinitionWrite.prototype['parentEntity'] = undefined;






export default DefinitionDefinitionWrite;

